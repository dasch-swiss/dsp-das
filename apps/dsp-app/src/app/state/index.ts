import { CurrentPageState } from './current-page/current-page.state';
import { ProjectsState } from './projects/projects.state';
import { UserState } from './user/user.state';

export const STATE_MODULES = [
    UserState,
    ProjectsState,
    CurrentPageState
];
