interface Ontology {
  name: string;
  label: string;
  comment: string;
}

interface DataModelClass {
  name: string;
  label: string;
  comment: string;
}

interface DataModelClassProperty {
  type: string;
  name: string;
  label: string;
  comment: string;
}
