dist: xenial
sudo: required

services:
    - docker

language: node_js

node_js:
  - "11"

cache:
  yarn: true
  directories:
  - node_modules

addons:
  chrome: stable

install:
  - yarn global add @angular/cli
  - cd $TRAVIS_BUILD_DIR
  - yarn install

script:
  - yarn test-ci
  - yarn run webdriver-update
  - yarn test-e2e-protractor
  - yarn build-prod
  - docker build .
