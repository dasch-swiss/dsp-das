import { TranslateSubclassOfPipe } from './translate-subclass-of.pipe';

describe('TranslateSubclassOfPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateSubclassOfPipe();
    expect(pipe).toBeTruthy();
  });
});
